export default function NotFound() {
  return (
    <div
      style={{
        color: "#fff",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      잘못된 페이지입니다.
    </div>
  );
}
